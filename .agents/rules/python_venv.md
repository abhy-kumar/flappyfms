# Python Virtual Environment Requirement

- Always use a Python virtual environment (`.venv`) whenever invoking Python commands, running Python scripts, or managing Python packages.
- Ensure commands run using `.venv\Scripts\python` or by activating the virtual environment first (`.venv\Scripts\Activate.ps1`).
